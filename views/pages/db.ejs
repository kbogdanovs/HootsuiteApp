<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

<% include ../partials/nav.ejs %>

<div class="container">
<h2>Database Results</h2>

<ul>
    <% results.forEach(function(r) { %>
        <li> <%= r.userId %> - <%= r.tweetId %> <%= r.created %> - <%= r.sourceLocale %> - <%= r.sourceText %></li>
    <% }); %>
</ul>

</div>

</body>
</html>
